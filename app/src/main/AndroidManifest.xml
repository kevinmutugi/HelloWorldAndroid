<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.helloworld" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActvity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AndroidSQLiteTutorialActivity"
            android:label="@string/title_activity_android_sqlite_tutorial" >
        </activity>
        <activity
            android:name=".main"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name=".photos_layout"
            android:label="@string/title_activity_photos_layout" >
        </activity>
        <activity
            android:name=".Videos"
            android:label="@string/title_activity_videos" >
        </activity>
    </application>

</manifest>
